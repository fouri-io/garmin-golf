# Credentials — never commit
.env

# macOS
.DS_Store

# Python
.venv/
__pycache__/
*.pyc
*.egg-info/
.ipynb_checkpoints/

# Garmin token store may also land here if GARMINTOKENS is set locally
.garminconnect/

# Data: the raw responses are the asset, but they're large/personal — keep local.
# Track the directory structure (.gitkeep) but ignore the contents.
data/raw/*
!data/raw/.gitkeep

# Processed round documents ARE versioned (opted in).
